apiVersion : v1
kind: Pod
metadata:
  name: nginx-pod
  labels: 
    env   : prod
    app   : main
    tier  : frontend
    owner : NikitaTertyschny
spec:
  containers:
    - name: container-nginx
      image: nginx:latest
      ports: 
        - containerPort: 80
